[pytest]
testpaths = test/equivalence
markers =
    fast: quick equivalence/unit checks, no heavy sim
    slow: end-to-end sim equivalence (seconds to minutes)
    golden: compares against captured golden baselines
addopts = -ra
